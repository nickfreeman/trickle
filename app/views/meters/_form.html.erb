<%= simple_form_for(@meter) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :serial %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
